# FAQ
Why JSON + SQLite? Portability + structured queries.
Standalone frontend? npm run dev (hostBridge guards).
Add section? Tables + SaveStructuredDataAsync + messages + types + UI.
Partial unit update? save-unit; global save rewrites.
ID handling? Preserve provided id else AUTOINCREMENT.
Backups? database/json/*.json alongside rts.db.
